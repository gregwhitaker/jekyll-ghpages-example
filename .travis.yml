language: java
sudo: false
jdk:
- oraclejdk8
install: true
before_install:
  gem install bundler jekyll
script: ./gradlew build
before_cache:
- rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
  - $HOME/.gradle/caches/
  - $HOME/.gradle/wrapper/
  - $HOME/.m2
